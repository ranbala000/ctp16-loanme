{{#each success}}
  <div class="alert alert-danger">{{this}}</div>
{{/each}}

<h1>{{user.username}}</h1>
<p>{{user.firstName}} {{user.lastName}}</p>

<div class="container">
  <div class="row">
    <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
      <p>Random Profile Info..</p>
    </div>
  </div>
</div>
